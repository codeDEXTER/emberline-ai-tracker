# Operating rules — moved

This file's content folded into `HANDOFF.md`'s "Operating rules, learned the
hard way" section on 2026-09-15 (proposal 23, M-11): one merged mandatory-read
file instead of two, with nothing lost — only relocated.

It is kept here, nearly empty, only because `.common-rules.json`'s
`read_order` and `CLAUDE.md` — both reserved to the sponsor, so this session
cannot edit them — still name this path. Read `HANDOFF.md` instead.
